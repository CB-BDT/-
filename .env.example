# ============================================================================
# 本扩展是纯前端 MV3 扩展（无服务端），运行时不需要读取任何环境变量：
# 所有第三方 Key（DeepL / 自定义 AI 模型）都由使用者在扩展「设置页」填写，
# 仅保存在其浏览器本地，不会上传到任何服务器，也不会写进源码或产物。
#
# 所以 .env 不是必需文件，仓库里也不存在任何真实凭证。
#
# 下面的变量仅供「想给自己的私有构建预置默认值」的人使用：
# 需要你自行在 src/core/config.ts 中读取后生效（默认为空）。
# ============================================================================

# 构建期预填的 DeepL API Key（免费版以 :fx 结尾）。
# 留空 = 使用者首次自行在设置页填写。切勿把真实 Key 提交到公开仓库。
PLASMO_PUBLIC_DEEPL_KEY=

# 首次安装时的默认翻译引擎：google / lingva / deepl / custom:<模型id>
PLASMO_PUBLIC_DEFAULT_ENGINE=google

# Lingva Translate 实例地址（社区公共实例失效时可换成自建/镜像）
PLASMO_PUBLIC_LINGVA_ENDPOINT=https://lingva.ml/api/v1
